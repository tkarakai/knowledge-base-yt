# Repository workflow

Read `CONTRIBUTING.md` before committing, pushing, or opening a PR.

- Work on a feature branch. If starting on `main`, create a branch before making changes or commits.
- Submit changes through a PR targeting `main`; never push directly to `main`.
- Run checks appropriate to the change and report remaining validation honestly. Wait for `Commonplace validation` before merging.
- Do not merge a PR unless the user authorizes merging. Squash and rebase merges are allowed; review outstanding conversations before merging.
- Administrators can bypass protection, matching `lifeor2-client`. Do not use that bypass unless the user explicitly requests it.

Product and implementation conventions are in `CLAUDE.md`.
